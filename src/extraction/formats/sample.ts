/**
 * Sample format-specific extractor
 * Create format-specific extractors in this directory
 *
 * Example: csv.ts, json.ts, xml.ts, etc.
 */

export function extractFromSampleFormat(_content: string): readonly string[] {
	// Implement format-specific extraction logic
	return Object.freeze([]);
}
